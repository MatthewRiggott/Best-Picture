Click on your favorite photo to vote!

<p>
  <% @photos.each do |photo| %>
   <div class="contest-image"><%= link_to image_tag(photo.url), contest_votes_path(@contest, photo: photo), method: :post, action: :create %></div>
  <% end %>
</p>

<%= link_to 'Back', contests_path %>
